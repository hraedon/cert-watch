# Source-tree fallback version, used only when installed package metadata is
# unavailable (running straight from a checkout). Installed builds derive the
# version from package metadata (pyproject); the Dockerfile also overwrites this
# file at build time with GIT_TAG + GIT_COMMIT to carry the commit hash.
0.9.3

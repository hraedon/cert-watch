apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cert-watch-demo-data
  namespace: cert-watch-demo
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi

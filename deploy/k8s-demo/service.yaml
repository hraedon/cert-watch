apiVersion: v1
kind: Service
metadata:
  name: cert-watch-demo
  namespace: cert-watch-demo
spec:
  selector:
    app.kubernetes.io/name: cert-watch-demo
  ports:
    - name: http
      port: 80
      targetPort: http

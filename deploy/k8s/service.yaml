apiVersion: v1
kind: Service
metadata:
  name: cert-watch
  namespace: cert-watch
spec:
  selector:
    app.kubernetes.io/name: cert-watch
  ports:
    - name: http
      port: 80
      targetPort: http

---
identifier: BC-144b
title: Test importlib.reload cleanup (~8 files)
kind: todo
status: open
severity: medium
---

## Problem

~8 individual test files still use inline `importlib.reload`. Some legitimately test import/lifespan behavior; the rest are a mechanical cleanup to remove unnecessary reloads now that `create_app(security=, auth_provider=, settings=)` supports clean injection (Plan 018).

## Suggested fix

Audit each inline reload, keep the ones that are testing import/lifespan semantics, and replace the rest with direct `create_app(...)` injection or the existing test fixtures.

## Related

- Split from BC-144
- Plan 018 (auth/data consolidation)

---
identifier: BC-143
title: No template or CSS linting in CI — runtime-only error detection
kind: improvement
status: new
severity: low
---

## Problem

There is no template or CSS linting in CI. Errors are only detected at runtime.

## Suggested fix

Add template linting (e.g., djlint) and CSS linting (e.g., stylelint) to CI.

---
identifier: BC-045
title: OAuth state smuggled in AuthResult.error field
kind: quality
status: resolved
severity: low
---

**Resolved:** 2026-05-30

## Problem

`OAuthProvider.start_oauth_flow()` returns the signed OAuth state in `AuthResult.error` because there is no dedicated field. The route handler (`routes/auth.py:115`) checks `if result.error:` to set the `cw_oauth_state` cookie. This is semantically misleading — `error` always holds a non-empty string on success. A future refactor could easily misinterpret this as a real error and skip setting the cookie, silently breaking OAuth login.

## Resolution

- Added `oauth_state: str = ""` to `AuthResult` dataclass (`auth.py`).
- Updated `OAuthProvider.start_oauth_flow()` to return `oauth_state=_sign_state(state)` instead of abusing the `error` field.
- Updated the route handler (`routes/auth.py`) to read `result.oauth_state` for the cookie.
- Existing tests remain green (398 passed).

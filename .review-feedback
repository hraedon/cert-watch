
=== REVIEW FEEDBACK (must address before merge) ===

A correctness review of your implementation found the following issues.
Fix ONLY these specific problems. Do not add new features or change unrelated code.
After each fix, commit it with: git add -A && git commit -m '<fr>: fix <issue>'
Re-run pytest after all fixes to verify nothing regressed.


CRITICAL (must fix — silent failure or data loss):
  [src/cert_watch/services/alert_service_impl.py:108] Logic bug in threshold evaluation: thresholds are checked in ascending order [14, 7, 3, 1] with 'days_remaining <= threshold'. A certificate with 2 days remaining triggers the 14-day threshold (2 <= 14) instead of the 3-day threshold. The loop breaks after first match, creating wrong-threshold alerts.
    → Fix: Reverse the threshold order to check [1, 3, 7, 14, 30] or use 'days_remaining >= threshold' logic to find the appropriate threshold. For 2 days remaining, should trigger 3-day threshold, not 14-day.
# Plan 046 — v0.6.5 Hardening / Truth-in-Advertising

> **Status:** in progress. The judgment-heavy "reserved" slice (A, B, C1, D1)
> is **done and merged to `main`** via PR #4 (merge `fde01e4`, 2026-06-07). The
> team-owned items (C2, D2, D3, backlog) and the release cut (E) **remain**.
> **Not tagged** — `main` is a clean development base, not a release yet.
>
> A deliberate feature-freeze hardening iteration before a future v1.0: no new
> product surface — close the gap between what cert-watch *appears* to do and
> what it *actually* does, plus a security pass and CI/release hygiene.

## Context

cert-watch reached reasonable feature-completeness at v0.6.0 (RBAC, API keys,
full E2E). The biggest remaining risk to its "Real Software" credibility is
**surfaces that look done but aren't**, and a backlog that drifted from reality.
v0.6.0 already surfaced two (RBAC inert; a test that was theater); the v0.6.5
review surfaced a third (RBAC inert *for AD specifically* — BC-150). This
iteration hunts those, hardens the credibility-facing paths, runs a security
review, and will cut a clean release with honest boundaries.

**Decision on file:** mis-issuance — removed the stubbed UI now, build it real
in 1.1 (BC-151).

## Division of labor

This plan was split deliberately (see PR #4 description):

- **Agent (reserved — judgment-heavy / credibility-defining / hard-to-reverse):**
  security review, the truth fixes, the Discover-load architecture, the visual
  gate decision, and the release cut.
- **Team (well-specified / mechanical / parallelizable):** the routine low-sev
  backlog and C2 / D2 / D3 below — each has a tracking breadcrumb and an existing
  test or a clear spec, so they can proceed independently.

## Workstreams

### A. Truth-in-advertising — ✅ DONE (PR #4)
- **A1 reconcile** — closed stale **BC-138** (`routes/api.py` was already
  decomposed). Auto-reconcile (`agent-notes breadcrumb reconcile`) found nothing
  else.
- **A2 Discover honesty (BC-099, BC-129)** — removed the always-empty
  Mis-issuance stat + "Mis-issuance watch" panel and the blank Issuer /
  first-seen columns from `templates/discover.html` + `routes/views.py`. Filed
  **BC-151** for real CT issuer/first-seen capture (1.1).
- **A3 claims audit** — swept README / CHANGELOG / UI. README already described
  Discover honestly and the compliance **CAA** metric is explicitly "Not
  collected"; **no over-claims to fix.**

### B. Security review — ✅ DONE (PR #4)
- Reviewed the v0.5.3→HEAD surface (API-key bearer auth, RBAC wiring, OAuth claim
  trust, SSRF opener, CSP/CSRF, migrations).
- **HIGH finding fixed — BC-150.** RBAC was silently **inert for real LDAP/AD**:
  group values are DNs with commas, and the session token's `,`-join encoding
  (`auth/session.py` `_encode_list`/`_decode_list`) shredded each DN into RDN
  fragments, so they never matched `CERT_WATCH_ROLE_MAP` → every AD user fell
  back to `viewer`. Same comma-split root cause as the old `LDAP_REQUIRED_GROUPS`
  bug, reintroduced in the BC-145 session propagation; the v0.6.0 RBAC E2E missed
  it by using comma-free names. **Fix:** lossless base64url(JSON) encoding;
  regression `tests/test_session_groups.py`; the RBAC browser E2E now uses real
  DN groups. Everything else reviewed clean (OAuth groups/roles are only trusted
  after JWKS/userinfo validation; API-key tokens are 256-bit, hashed-at-rest,
  rate-limited).

### C. Performance / correctness on credibility paths
- **C1 Discover load (BC-097)** — ✅ DONE (PR #4). `discover_view` no longer
  blocks on live crt.sh (15s timeout × N domains). It renders from the
  reconciliation cache (`ct_monitor.peek_reconciliation`, no I/O) and warms
  stale/missing domains off-thread (`start_reconciliation_refresh`), with a
  "reconciling…" / "updated Ns ago" indicator. Tests:
  `tests/test_discover_decouple.py`.
- **C2 Compliance report (BC-120, BC-122)** — ⬜ TEAM. In `compliance.py`,
  replace per-cert `get_posture_for_cert` with the batch `get_posture_for_certs`
  (exported from `cert_watch.database`) to kill the N+1; bound the report query
  instead of `list_dashboard_rows`-loading everything. Existing compliance tests
  guard output parity. (BC-121 CAA collection is **1.1**, not here — the report
  is already honest about it.)

### D. CI / release hygiene
- **D1 Visual gate** — ✅ DONE (PR #4). Promoted the `visual` job in `e2e.yml`
  from `continue-on-error` to a **required** gate (matched ubuntu-latest 4/4 at
  the 0.2 threshold). Deliberate UI changes re-baseline with
  `pytest -m visual --update-snapshots`.
- **D2 Action deprecations (BC-152)** — ⬜ TEAM. Bump `astral-sh/setup-uv` (and
  any other Node-20 actions) to Node-24-capable pins across all
  `.github/workflows/` (GitHub removes Node-20 on 2026-06-16).
- **D3 Audit coverage (BC-153)** — ⬜ TEAM. `pip-audit` only covers the `test`
  job's `.[dev,auth]`; add a lightweight audit of the e2e extra deps
  (pillow/pixelmatch/visual-snapshot).

### E. Cut v0.6.5 — ⬜ REMAINING (agent, after C2/D2/D3 land)
- CHANGELOG `[0.6.5]` (truth fixes, BC-150 security fix — note RBAC now works for
  AD, Discover/compliance perf, CI hygiene).
- README **Known limitations / Non-goals** section (CT mis-issuance is 1.1,
  single-writer SQLite, CAA not stored per scan, …) — honest boundaries set up
  v1.0.
- Bump version `0.6.0 → 0.6.5` (`pyproject.toml` + `src/cert_watch/_version.txt`),
  merge to `main`, tag `v0.6.5` (triggers `release.yml` image build + Trivy).

## Breadcrumb map

| Item | Breadcrumb | Status |
|---|---|---|
| api.py decompose (stale) | BC-138 | ✅ resolved |
| Discover mis-issuance / first-seen stub | BC-099 / BC-129 | ✅ resolved |
| RBAC group-DN shredding | BC-150 | ✅ resolved |
| Discover blocks on crt.sh | BC-097 | ✅ resolved |
| Real CT mis-issuance detection | BC-151 | ⬜ 1.1 |
| Compliance N+1 / unbounded | BC-120 / BC-122 | ⬜ team (C2) |
| Node-20 action pins | BC-152 | ⬜ team (D2) |
| e2e pip-audit coverage | BC-153 | ⬜ team (D3) |
| CAA per-scan collection | BC-121 | ⬜ 1.1 |

## Out of scope (→ 1.1 / later)

Real CT mis-issuance + first-seen (Plan 040 / BC-151), CAA per-scan (BC-121),
policy engine (042), Postgres (043), webhook streaming (044), and the routine
low-severity backlog.

## Verification (gate before E)

Docker is available locally, so the Samba integration tier runs.

- `.venv/bin/ruff check .` and `.venv/bin/mypy src/cert_watch` → clean.
- `.venv/bin/pytest -q --ignore=tests/e2e --ignore=tests/integration` → ≥88% cov
  (1324 passed as of PR #4).
- `.venv/bin/pytest -m "e2e and not visual" tests/e2e -n0 --no-cov` (35) and
  `.venv/bin/pytest -m visual tests/e2e -n0 --no-cov` (8 baselines).
- `.venv/bin/pytest -m integration tests/integration -n0 --no-cov` (Samba).
- On the PR/`main`: `ci`, `e2e`, and the now-required `visual` job green.
- Release (E): `v0.6.5` tagged; `release.yml` image build + Trivy pass.
